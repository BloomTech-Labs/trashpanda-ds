#!/bin/bash
# Descends into "images" folder and removes all .txt files 
# (generated from labeling from multifile_label.py)

# DANGEROUS if we don't want to lose generated .txt files
#rm $(find images/ -type f | grep .txt)

